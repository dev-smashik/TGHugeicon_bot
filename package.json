{
  "name": "hugeicons-telegram-bot-vercel",
  "version": "1.0.0",
  "description": "Serverless Telegram Bot for HugeIcons & FlatIcon",
  "main": "api/webhook.js",
  "scripts": {
    "start": "node api/webhook.js",
    "build": "echo 'No build required for serverless API'",
    "dev": "node --watch api/webhook.js --local",
    "set-webhook": "node scripts/set-webhook.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^17.3.1",
    "node-telegram-bot-api": "^0.66.0"
  }
}
